package entity.unit;

import javafx.scene.image.Image;

public class DoubleCarrot extends Projectile{
    public DoubleCarrot(double startX, double startY, Image image, double angle, double power) {
        super(startX, startY, image, angle, power);
    }
}
