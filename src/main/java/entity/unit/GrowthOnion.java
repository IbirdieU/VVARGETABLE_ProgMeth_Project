package entity.unit;

import javafx.scene.image.Image;

public class GrowthOnion extends Projectile{
    public GrowthOnion(double startX, double startY, Image image, double angle, double power) {
        super(startX, startY, image, angle, power);
    }
}
