package entity.unit;

import javafx.scene.image.Image;

public class ToxicOnion extends Projectile{
    public ToxicOnion(double startX, double startY, Image image, double angle, double power) {
        super(startX, startY, image, angle, power);
    }
}
